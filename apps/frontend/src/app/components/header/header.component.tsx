import { Logo } from '@components/logo';
import { HeaderContainer } from './header.styles';

const Header = () => {
  return (
    <HeaderContainer>
      <Logo />
    </HeaderContainer>
  );
};

export default Header;
